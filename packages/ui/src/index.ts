// Shared design-system components (RFC-004 §4). Populated when real UI is
// built — see this package's README. Only the shadcn cn() utility exists
// so far, since it's infrastructure every future component needs.
export * from "./lib/cn.js";
