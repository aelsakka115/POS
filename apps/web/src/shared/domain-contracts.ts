// Keep shared API types anchored to the repository's single contract package.
// This is a type-only re-export, so it adds no browser runtime dependency.
export type { TenantScoped } from "@cafe-engine/domain-contracts";
